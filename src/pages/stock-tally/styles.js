import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    root: {
        padding: '10px 40px'
    },
    paper: {
        padding: '30px 20px',
        marginBottom: '20px'
    },
    card: {
        padding: '10px 20px'
    }
}));
